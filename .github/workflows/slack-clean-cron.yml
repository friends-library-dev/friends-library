name: slack-clean

on:
  schedule:
    - cron: 0 0,12 * * *

jobs:
  clean:
    runs-on: ubuntu-latest
    steps:
      - name: set-env
        run: |
          echo ::set-env name=FIVE_DAYS_AGO::$(date -d '-5 days' '+%Y%m%d')
          echo ::set-env name=THREE_DAYS_AGO::$(date -d '-3 days' '+%Y%m%d')
          echo ::set-env name=ONE_DAY_AGO::$(date -d '-1 days' '+%Y%m%d')
      - name: clean downloads channel
        uses: matken11235/slack-cleaner@v1
        with:
          group: downloads
          before: ${{ env.FIVE_DAYS_AGO }}
          rate: '1'
          message: true
          bot: true
          perform: true
          token: ${{ secrets.SLACK_LEGACY_TOKEN }}
      - name: clean errors channel
        uses: matken11235/slack-cleaner@v1
        with:
          group: errors
          before: ${{ env.FIVE_DAYS_AGO }}
          rate: '1'
          message: true
          bot: true
          perform: true
          token: ${{ secrets.SLACK_LEGACY_TOKEN }}
      - name: clean msf-errors channel
        uses: matken11235/slack-cleaner@v1
        with:
          group: msf-errors
          before: ${{ env.THREE_DAYS_AGO }}
          rate: '1'
          message: true
          bot: true
          perform: true
          token: ${{ secrets.SLACK_LEGACY_TOKEN }}
      - name: clean fnlogs channel
        uses: matken11235/slack-cleaner@v1
        with:
          group: fnlogs
          before: ${{ env.ONE_DAY_AGO }}
          rate: '1'
          message: true
          bot: true
          perform: true
          token: ${{ secrets.SLACK_LEGACY_TOKEN }}
      - name: clean dev channel
        uses: matken11235/slack-cleaner@v1
        with:
          channel: dev
          before: ${{ env.FIVE_DAYS_AGO }}
          rate: '1'
          message: true
          bot: true
          perform: true
          token: ${{ secrets.SLACK_LEGACY_TOKEN }}
      - name: clean test-bot-channel
        uses: matken11235/slack-cleaner@v1
        with:
          group: test-bot
          before: ${{ env.ONE_DAY_AGO }}
          rate: '1'
          message: true
          bot: true
          perform: true
          token: ${{ secrets.SLACK_LEGACY_TOKEN }}
