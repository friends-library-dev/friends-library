{
  "name": "@friends-library/hilkiah",
  "version": "1.0.0",
  "description": "finds scripture references embedded in text",
  "main": "dist/index.js",
  "author": "jared@netrivet.com",
  "license": "MIT",
  "scripts": {
    "install": "yarn build",
    "build": "yarn run clean && yarn tsc -p .",
    "clean": "rm -rf ./dist"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/jest": "^24.0.11",
    "ts-jest": "^24.0.0",
    "typescript": "^3.3.3333"
  }
}
