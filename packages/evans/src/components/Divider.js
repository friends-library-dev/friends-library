// @flow
import * as React from 'react';

// this component uses non-emotion css (from Layout.css)
// because it's embedded in translated asciidoc partials
// if that ever becomes not the case, switch back to emotion

export default () => <hr className="Divider" />;
