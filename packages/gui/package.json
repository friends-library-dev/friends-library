{
  "name": "@friends-library/gui",
  "version": "1.0.0",
  "private": true,
  "main": "src/electron-main.js",
  "dependencies": {
    "concurrently": "^4.1.0",
    "react": "^16.6.3",
    "react-dom": "^16.6.3",
    "react-scripts": "2.1.1"
  },
  "scripts": {
    "develop": "concurrently --kill-others \"yarn start\" \"sleep 5 && yarn electron\"",
    "start": "SKIP_PREFLIGHT_CHECK=true BROWSER=none react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "electron": "ELECTRON_START_URL=http://localhost:3000 electron ."
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ],
  "devDependencies": {
    "electron": "^3.0.13"
  }
}
