import css from './tmpl';

const authorCss: string = css`
  .author {
  }

  .author__line {
    width: 40%;
    height: 0.015in;
    background: white;
    opacity: 1;
    margin: 0 auto 0 auto;
  }

  .author__name {
    font-weight: 400;
    font-size: 0.19in;
    opacity: 0.7;
  }
`;

export default authorCss;
