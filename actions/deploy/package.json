{
  "name": "@friends-library/deploy",
  "version": "1.0.0",
  "author": "Jared Henderson <jared@netrivet.com>",
  "license": "MIT",
  "scripts": {
    "start": "ncc build index.ts -o bundled -w -e typescript",
    "build": "ncc build index.ts -o bundled -m -e typescript"
  },
  "dependencies": {
    "@actions/core": "^1.2.4",
    "@octokit/action": "^2.7.1",
    "netlify": "^4.3.0"
  }
}
